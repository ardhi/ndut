const start = require('./src/start.js')
const getConfig = require('./src/util/get-config')
const init = require('./src/init')
const run = require('./src/run')

module.exports = { start, getConfig, run, init }
